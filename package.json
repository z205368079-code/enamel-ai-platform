{
  "name": "enamel-ai-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Interview PoC for an enamel cookware enterprise AI integration platform.",
  "workspaces": [
    "apps/*"
  ],
  "engines": {
    "node": ">=22 <27"
  },
  "scripts": {
    "build": "npm run build --workspace @enamel-ai/gateway",
    "dev": "npm run dev --workspace @enamel-ai/gateway",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "npm run lint --workspace @enamel-ai/gateway",
    "start": "npm run start --workspace @enamel-ai/gateway",
    "test": "npm run test --workspace @enamel-ai/gateway",
    "typecheck": "npm run typecheck --workspace @enamel-ai/gateway",
    "verify": "npm run format:check && npm run lint && npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.0",
    "@types/express": "^5.0.3",
    "@types/node": "^24.3.0",
    "@types/supertest": "^6.0.3",
    "eslint": "^10.0.0",
    "globals": "^16.3.0",
    "prettier": "^3.6.2",
    "supertest": "^7.1.4",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.42.0",
    "vitest": "^3.2.4"
  }
}
